# ywr-to-markers
This tool can be used to convert .ywf files to position coordinates that can be displayed in codewalker using the Multi-find box in the Markers tab.

The script will read the YWF positions from a text file called 'input.txt' located in the same directory as the script is being ran. The output will be printed out into the console.
